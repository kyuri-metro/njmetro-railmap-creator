import { COMPACT_MAX_WIDTH_PX } from './breakpoints';

export { COMPACT_MAX_WIDTH_PX as TOPBAR_COMPACT_MAX_WIDTH_PX } from './breakpoints';

export const topbarCompactMediaQuery = `(max-width: ${COMPACT_MAX_WIDTH_PX}px)`;
