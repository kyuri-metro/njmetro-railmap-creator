/** 顶栏收窄断点（px）：与 `styles.css` 中 `@media (max-width: …)` 保持一致。 */
export const TOPBAR_COMPACT_MAX_WIDTH_PX = 720;

export const topbarCompactMediaQuery = `(max-width: ${TOPBAR_COMPACT_MAX_WIDTH_PX}px)`;
