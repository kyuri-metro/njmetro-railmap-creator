/** App layout breakpoints (px). Keep in sync with CSS media queries under src/styles. */

/** Topbar / form compact layout; CSS `@media (max-width: 720px)` and `(min-width: 721px)`. */
export const COMPACT_MAX_WIDTH_PX = 720;

/** Narrow toolbar / example trigger; CSS `@media (max-width: 640px)`. */
export const NARROW_MAX_WIDTH_PX = 640;

/** Two-column stack; CSS `@media (max-width: 900px)`. */
export const COLUMNS_STACK_MAX_WIDTH_PX = 900;

/** Dialog field stacking; CSS `@media (max-width: 420px)`. */
export const DIALOG_NARROW_MAX_WIDTH_PX = 420;

/** `@container app-layout (min-width: 1108px)` fixed left column threshold. */
export const APP_LAYOUT_FIXED_SIDEBAR_MIN_WIDTH_PX = 1108;
